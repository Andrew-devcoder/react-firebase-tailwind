import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

const yourSlice = createSlice({
	name: 'yourSlice',
	initialState,
	reducers: {},
});

export const {} = yourSlice.actions;
export default yourSlice;
